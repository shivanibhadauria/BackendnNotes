<!DOCTYPE html>
<html>
  <head>
    <title><%= filename %> - Task Manager</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 text-white font-sans">
    <div class="main w-full min-h-screen flex flex-col items-center justify-center p-6">

      <!-- Task Details -->
      <div class="task-details w-full max-w-md bg-zinc-800 rounded-2xl shadow-lg p-6">
        <h1 class="text-2xl font-bold mb-4"><%= filename %></h1>
        <p class="text-gray-300 mb-6 whitespace-pre-line"><%= filedata %></p>

        <div class="flex justify-between">
          <a href="/" 
             class="bg-indigo-600 hover:bg-indigo-500 text-white font-medium py-2 px-4 rounded-lg transition">
            ⬅ Back to Home
          </a>

          <div class="flex gap-2">
            <button class="edit-btn bg-yellow-500 hover:bg-yellow-400 text-white font-medium py-2 px-4 rounded-lg">Edit</button>
            <button class="delete-btn bg-red-500 hover:bg-red-400 text-white font-medium py-2 px-4 rounded-lg">Delete</button>
          </div>
        </div>
      </div>

    </div>
  </body>
</html>
