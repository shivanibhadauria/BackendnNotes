<!DOCTYPE html>
<html>
  <head>
    <title><%= filename %> - Task Manager</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 text-white font-sans">
    <div class="main w-full min-h-screen flex flex-col items-center justify-center p-6">

      <!-- Task Details -->
      <div class="task-details w-full max-w-md bg-zinc-800 rounded-2xl shadow-lg p-8 space-y-6">
       
        <!-- Back Button -->
        <a href="/" 
           class="inline-block bg-indigo-600 hover:bg-indigo-500 text-white font-medium py-2 px-4 rounded-lg transition">
          ⬅ Back to Home
        </a>

        <!-- Edit Form -->
        <form action="/edit" method="post" class="space-y-4">
          
          <!-- Previous Name -->
          <div>
            <label class="block text-sm font-medium mb-1">Previous Name</label>
            <input name="previous"
              class="w-full px-4 py-2 rounded-lg bg-zinc-700 text-white placeholder-gray-400 focus:ring-2 focus:ring-indigo-500 outline-none" 
              type="text" 
              placeholder="Previous Name" 
              value="<%= filename %>">
          </div>

          <!-- New Name -->
          <div>
            <label class="block text-sm font-medium mb-1">New Name</label>
            <input name="next"
              class="w-full px-4 py-2 rounded-lg bg-zinc-700 text-white placeholder-gray-400 focus:ring-2 focus:ring-indigo-500 outline-none" 
              type="text" 
              placeholder="Enter new name">
          </div>

          <!-- Submit Button -->
          <div>
            <button type="submit"
              class="w-full bg-green-600 hover:bg-green-500 text-white font-semibold py-2 px-4 rounded-lg transition shadow-md">
              ✅ Save Changes
            </button>
          </div>

        </form>
     
      </div>
    </div>
  </body>
</html>
