<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Users</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap');
      body {
        font-family: 'Poppins', sans-serif;
      }
    </style>
  </head>
  <body class="bg-gray-950 text-gray-200 min-h-screen flex flex-col items-center">
    <div class="container mx-auto py-12 px-6 lg:px-8 max-w-7xl">
      <a href="/" class="text-indigo-400 hover:text-indigo-300 transition-colors duration-200 flex items-center mb-10">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd" />
        </svg>
        Back to Home
      </a>

      <h1 class="text-5xl font-extrabold mb-12 text-center text-white">Our Community</h1>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
        <% users.forEach(user => { %>
        <div class="bg-gray-900 border border-gray-800 rounded-2xl p-8 shadow-2xl transition-all duration-300 hover:shadow-indigo-500/20 hover:scale-105">
          <div class="flex flex-col items-center">
            <img src="<%= user.image %>" alt="User Avatar" class="w-32 h-32  mb-6 border-4 border-indigo-500 object-cover shadow-lg">
            <h2 class="text-3xl font-bold text-white text-center mb-1"><%= user.name %></h2>
            <p class="text-gray-400 text-sm text-center mb-6"><%= user.email %></p>
            <div class="flex space-x-4 mt-auto w-full">
              <form action="/update/<%= user._id %>" class="flex-1">


                <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-3 rounded-xl transition-colors duration-200 shadow-md hover:shadow-lg">Edit</button>
              </form>
              <form action="/delete/<%= user._id %>" method="post" class="flex-1">
                <button type="submit" class="w-full bg-red-600 hover:bg-red-700 text-white font-semibold py-3 rounded-xl transition-colors duration-200 shadow-md hover:shadow-lg">Delete</button>
              </form>
            </div>
          </div>
        </div>
        <% }); %>
      </div>
    </div>
  </body>
</html>